# ---------------------------------------------------------------------------
# Flask
# ---------------------------------------------------------------------------

# Controls which environment the app runs in (defaults to 'production').
# This also determines which SSM parameters are used at startup:
#
#   development -> devISSU-admin-connect-string  / db: dev_undlFiles
#   production  -> prodISSU-admin-connect-string / db: undlFiles
#
FLASK_ENV=development
